[project]
name = "adb"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["httpx>=0.27", "orjson>=3.10", "rich>=13.0"]

[project.scripts]
adb = "adb:main"

[tool.setuptools]
py-modules = ["adb"]
